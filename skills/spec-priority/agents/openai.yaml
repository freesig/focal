interface:
  display_name: "Spec Priority"
  short_description: "Review one rework-risk spec item"
  default_prompt: "Use $spec-priority to identify the single highest-priority review item in the attached spec excerpt, collect my decision, then continue one item at a time until I say done."
