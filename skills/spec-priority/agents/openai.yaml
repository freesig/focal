interface:
  display_name: "Spec Priority"
  short_description: "Review rework-risk spec items"
  default_prompt: "Use $spec-priority to identify the highest-priority review items in the attached spec excerpt, collect my decisions, and update the spec when I say done."
