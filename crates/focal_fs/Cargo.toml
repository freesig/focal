[package]
name = "focal-fs"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "focal_fs"
path = "src/lib.rs"

[dependencies]
pathdiff = "0.2"
thiserror = "2"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
tempfile = "3"
